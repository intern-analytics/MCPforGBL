{
  "brand_id": "rainbow_brands",
  "display_name": "Rainbow Brands Database Access",
  "tool_name": "Rainbow_execute_query",
  "allowed_db_user": [
    "powerbi_readonlyuser"
  ],
  "description": "Execute raw PostgreSQL queries against the Rainbow Brands database.",
  "specific_instructions": "You are an expert SQL analyst for Rainbow Brands. You have direct access to run raw SELECT queries against the database to fetch data. Always write valid PostgreSQL syntax.",
  "schema_details": "Standard PostgreSQL schema. You can run standard system queries if you need to explore tables.",
  "common_questions": [
    "Run a test query to verify database access."
  ]
}